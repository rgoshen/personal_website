export interface Project {
  title: string;
  description: string;
  link: string;
  imageSrc?: string; // Optional image for the project
  githubLink?: string; // Optional GitHub link for personal projects
}
